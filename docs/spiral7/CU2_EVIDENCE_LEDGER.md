# Spiral 7 CU2 evidence ledger

| Evidence | Proves | Does not prove |
|---|---|---|
| CU2 architecture manifest | Frozen CU2 dimensions, extension boundary and deferred work | Runtime Candidate selection |
| Sparse–Temporal Delta Semantic bytes | Canonical `A_t`, `M_t`, derived-set identities and per-item generations | Independent Verifier authority |
| Compact Delta artifact bytes | Exact sorted Update/Clear list, fixed tail and prior-extension identity binding | Resource and device-epoch binding |
| WARP architecture evidence | Update `W_t`, Clear `R_t`, untouched `H_t`, current Active oracle and inactive sentinel | Real hardware performance |
| Debug/Release byte comparison | Deterministic CU2 report within the qualified WARP boundary | Cross-machine identity |
| Negative gates | malformed sets, illegal modified membership, corruption and cross-invocation replay are rejected | CU3 mutation matrix completeness |

## CU2 proof boundary

CU2 establishes the first executable relation across Sparse membership, Temporal History and indirect quantity:

```text
T_t = W_t union R_t
```

It proves one direct artifact path only. Raw Candidate, Planner-independent Verifier, opaque seal and device-epoch authority remain CU3 obligations.

```text
RuntimePolicyAuthorization = None
```
