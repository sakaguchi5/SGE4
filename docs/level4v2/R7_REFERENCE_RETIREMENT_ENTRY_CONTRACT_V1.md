# Level 4 v2 R7 reference retirement entry contract V1

R7 may begin only after R6 proves all of the following:

- all sixteen frozen source roles are represented;
- all forty R0 carried Invariants have a terminal migration outcome;
- the representative/adversarial Migration Corpus passes in Debug and Release;
- correctness and observational performance Evidence remain separate;
- no Runtime performance policy was authorized.

R7 is an Owner decision, not an automatic cleanup pass. For each retained reference Project or document family, the Owner must choose and record exactly one disposition:

```text
RetainAsPermanentReference
ArchiveOutsideActiveSolution
DeleteAfterReplacementProof
```

R7 may not remove raw external Evidence required by the accepted research record. It may not reinterpret R6 success as permission to remove material without a per-family disposition and dependency audit.
