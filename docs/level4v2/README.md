# Level 4 v2 Canonical reconstruction

This directory is the active reconstruction entry point after closure of the exploratory program.

```text
R0 Input Freeze = PASSED
R1 Canonical Vocabulary = PASSED
R2 Unified Authority Chain = PASSED
R3 Canonical Composition = PASSED
R4 Dynamic Invocation and History = COMPLETE PACKAGE SUPPLIED; OWNER GATE PENDING
R5 Runtime and Recovery = NEXT AFTER R4 ACCEPTANCE
Runtime Candidate-policy authorization = None
```

Active command:

```powershell
.\run_sge4_5_level4v2_r4_dynamic_invocation_history.bat
```

Read `R4_DYNAMIC_INVOCATION_AND_HISTORY.md`, `R4_AUTHORITY_OWNERSHIP_V1.md`, `R4_MUTATION_REPLAY_MATRIX_V1.md`, `R4_EVIDENCE_LEDGER_V1.md`, then `R5_RUNTIME_RECOVERY_ENTRY_CONTRACT_V1.md`.

Hard boundaries remain: no hardware winner in the Canonical ABI, no application policy, no unproven graph generalization, no native Runtime authority in R4, and no reference-Project deletion before migration proof.
