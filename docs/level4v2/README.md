# Level 4 v2 Canonical reconstruction

This directory is the active reconstruction entry point after closure of the exploratory program.

```text
R0 Input Freeze = PASSED
R1 Canonical Vocabulary = PASSED
R2 Unified Authority Chain = PASSED
R3 Canonical Composition = PASSED
R4 Dynamic Invocation and History = PASSED
R5 Runtime and Recovery = COMPLETE PACKAGE SUPPLIED; OWNER GATE PENDING
R6 Migration Corpus = NEXT AFTER R5 ACCEPTANCE
Runtime Candidate-policy authorization = None
```

Active command:

```powershell
.\run_sge4_5_level4v2_r5_runtime_recovery.bat
```

Read `R5_RUNTIME_AND_RECOVERY.md`, `R5_AUTHORITY_OWNERSHIP_V1.md`, `R5_RECOVERY_STATE_MACHINE_V1.md`, `R5_EVIDENCE_LEDGER_V1.md`, then `R6_MIGRATION_CORPUS_ENTRY_CONTRACT_V1.md`.

Hard boundaries remain: one Adapter, whole-composition Recovery, no Runtime Candidate-performance policy, no unproven graph generalization, and no reference-Project deletion before R6 migration proof.
