# Level 4 v2 reconstruction roadmap V1

## R0 — Input freeze — PASSED

Freeze the Level 4 v1 and Spiral 1–7 closure sources, accepted evidence identities, proof ledgers and the no-new-capability rule.

Accepted completion evidence:

```text
40 carried invariants
40 explicit owners
40 intended v2 destinations
16 frozen source files
R1 entry contract frozen
C++/solution mutation: None
Accepted R0 commit: b8b5b4f675e4186a3ae202d718e091b63c53e264
```

## R1 — Canonical vocabulary — COMPLETE PACKAGE SUPPLIED; OWNER GATE PENDING

Define neutral types for Semantic meaning, dynamic invocation inputs, Candidate proposals, opaque Verified Plans, Frozen artifacts, history validity and Device epoch. Remove staged experiment names from the public model.

Supplied completion evidence:

```text
11 strong identity concepts
8 strong dynamic concepts
Raw Candidate -> opaque Verified Plan construction unavailable
Canonical public source has no Backend dependency
Generic vocabulary has no universal qualification limits
Debug/Release deterministic vocabulary evidence required
Runtime capability added: None
```

Successful Owner execution advances to R2.

## R2 — Unified authority chain — NEXT

Reconstruct Planner-independent verification and Frozen identity without duplicating the same fact across layers.

Completion: mutation and replay matrices from Spirals 1–7 map to one coherent verifier boundary.

## R3 — Canonical composition

Reconstruct the proven finite Buffer DAG subset, including explicit flow, allocation, scheduling, state and synchronization authority.

Completion: Level 4 v1 corpus and selected Spiral compositions migrate without Backend judgment.

## R4 — Dynamic invocation and history

Integrate verified indirect quantity, temporal history, exact sparse sets and versioned delta transitions as explicit dynamic inputs and artifacts.

Completion: Spiral 4–7 qualification corpora pass through v2 types.

## R5 — Runtime and Recovery

Reconstruct materialization, epoch-bound handles, explicit external rebind, full-active Recovery seed and removed-Adapter exclusion.

Completion: Controlled Recovery, actual RemoveDevice and fresh-process evidence are reproduced or explicitly versioned.

## R6 — Migration corpus

Run representative and adversarial cases from every Spiral. Keep experiment performance suites separate from the Canonical correctness gate.

Completion: all 40 carried invariants have terminal migration outcomes; unresolved differences are documented rather than hidden.

## R7 — Reference retirement decision

Only after R0–R6 may old Spiral Projects be removed, archived outside the active solution, or retained as permanent examples.

Completion: a file-by-file retirement manifest exists and the normal v2 gate no longer depends on staged CU layout.
