# Spiral 2 Stage 21 Decision Evidence

The Release measurement completed on `NVIDIA GeForce RTX 4070 Laptop GPU`.
M0-M5 each contain 96 recorded balanced-order samples after warm-up. Every
sample uses one of `ABC ACB BAC BCA CAB CBA`, explicit completion between order
steps, the same topology and F10 invocation for its case, the D3D12 Direct queue
contract, and hardware timestamp queries. WARP is excluded from performance
claims.

Aggregate sample-median GPU ranking emitted by the executable report:

1. `B.DirectPgaHierarchy`: 49,152 ns
2. `A.MatrixHierarchy`: 140,288 ns
3. `C.HybridHierarchy`: 143,360 ns

`RecommendationAuthority = NonAuthoritative`

Candidate B is the measured-adapter representation recommendation. It is not a
global policy selection. For the next capability, `Vertex Consumer` is the
strongest non-authoritative evidence direction because qualified global
transforms currently feed only the fixed probe consumer.

The generated report at
`build/tests/spiral2/measurement/SPIRAL2_DECISION_EVIDENCE_REPORT.md` answers all
twelve roadmap questions and records candidate generation, independent
Verifier, Freeze, Package bytes, Dynamic upload, logical intermediate
allocation, materialization, command recording, per-Leaf and total GPU,
end-to-end, dispatch, barrier, and numeric error evidence. The binary evidence
and raw CSV remain beside the report.

Summary of the twelve answers:

1. Level 4 v1 closed Dynamic Palette Composition without extension.
2. No Frozen Level 4 contract was missing.
3. Per-frame Motor values remained outside Frozen identity.
4. One Semantic produced three materially distinct verified Leaf groups.
5. Matrix lowering before, absent, and after hierarchy changed dispatch and intermediate layouts.
6. Bone count and depth effects remain case-local in the measured table.
7. The 32-byte Motor / 48-byte Matrix difference is preserved in resource evidence.
8. Direct PGA traded retained arithmetic for fewer dispatches and smaller intermediates.
9. Hybrid used Motor hierarchy followed by global Matrix conversion.
10. Verifier mutation evidence covered Leaf graph and lowering boundaries.
11. Vertex Consumer is recommended only as non-authoritative evidence.
12. No Level 4 extension is experimentally required or authorized.

`NextCapabilitySelection = DeferredByOwner`

`SelectionStatus = OWNER_DECISION_REQUIRED`
