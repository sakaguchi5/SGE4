# SGE4-5 Spiral 3 — CU0/CU1/CU2

Baseline: `398891936bf498fed532b26c06884bbfbf66f3a8`

Qualified Completion Units:

- CU0 freezes the fixed-count reuse sweep research contract, ABI policy and project boundaries.
- CU1 implements the fixed eight-bone hierarchy, six explicit reuse workloads, deterministic point corpus and independent binary64 CPU reference.
- CU2 generates Matrix-before, Direct-PGA and Matrix-after graph candidates for every reuse workload, then independently rederives and seals only valid plans.

Run the new unit after CU0 and CU1:

```bat
run_sge4_5_spiral3_cu2_candidate_authority.bat
```

CU3 and later projects are intentionally not included in this delivery.
