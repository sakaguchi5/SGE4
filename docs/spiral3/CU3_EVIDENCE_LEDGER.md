# Spiral 3 CU3 evidence ledger

Scope: Verified Representation to Schema 17 Frozen Leaf authority.

Implemented evidence:

- the Leaf Compiler has no ProjectReference to the Planner and its public API accepts only `VerifiedReuseRepresentationV1`;
- six reuse cases each lower common Dynamic/Point sources plus Matrix-before, Direct-PGA and Matrix-after candidate groups;
- 18 verified candidate groups become 60 Package-bound Leaves in total;
- Matrix/PGA representation stride, consumer point count, dispatch and materialization position are bound to the verified operation evidence;
- fixed local-point corpus bytes are Package-owned initial data, while per-frame Motor and binary32 reference bytes remain DynamicData slots;
- Package interface, binding layout, shader bytecode, execution digest and file digest are rebound into a Spiral 3 Leaf identity;
- 80 plan, operation, Program, workload, dispatch and digest mutations are rejected;
- Debug fresh-process A/B and Release evidence must be byte-identical;
- D3D12 Schema 17 / Runtime 17 remain unchanged.

Windows completion command: `run_sge4_5_spiral3_cu3_leaf_groups.bat`.
