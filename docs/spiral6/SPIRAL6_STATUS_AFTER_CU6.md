# Spiral 6 status after CU6

Successful execution of the CU6 runner declares:

```text
SGE4-5 SPIRAL 6 EXPERIMENT COMPLETE
```

The declaration means that the exact Sparse Candidate family has completed Architecture qualification and real-GPU `(Pattern,K)` measurement with formal Decision Evidence.

It does not declare a universal winner or authorize adaptive Runtime selection.

The retained boundary is:

```text
RuntimePolicyAuthorization = None
NextCapabilitySelection = DeferredByOwner
SelectionStatus = OWNER_DECISION_REQUIRED
```

`SPIRAL 6 CLOSED` remains withheld until the Owner selects the next capability or explicitly closes the research line.
