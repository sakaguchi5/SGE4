# Spiral 6 stage map V1

## CU1 — Research Contract Freeze

Owner selection, Spiral 5 closure, exact sparse-set meaning, corpus, Candidate family, boundaries and proof ledger. Documents and verification only.

## CU2 — Compact Index List Architecture

Implement canonical Sparse Semantic, one `CompactIndexListDispatch` path and the minimal versioned Sparse Work-Set extension. Prove baseline insufficiency before extension.

## CU3 — Independent Sparse Authority

Split Raw Candidate, Planner and Planner-independent Verifier. Bind set identity, representation role, Programs, Resources, Target and device epoch. Reject mutation and replay attacks.

## CU4 — Sparse Candidate Family

Add `DenseMembershipMaskPredicate` and `ActiveBlockListLocalMask`. Prove A/B/C exact write-set and full-output equivalence.

## CU5 — Architecture Qualification

Run all qualification cardinalities and patterns on WARP. Prove inactive sentinel preservation, zero-work behavior, determinism, controlled Recovery, actual Device Removal and explicit sparse rebind/rebuild.

May declare:

```text
SGE4-5 SPIRAL 6 ARCHITECTURE COMPLETE
```

## CU6 — Real-GPU Measurement and Decision Evidence

Measure all Candidates across measurement cardinalities and pattern constructors with balanced order. Generate binary evidence and `(Pattern,K)` Decision Report.

May declare:

```text
SGE4-5 SPIRAL 6 EXPERIMENT COMPLETE
```

`SPIRAL 6 CLOSED` remains Owner-gated.
