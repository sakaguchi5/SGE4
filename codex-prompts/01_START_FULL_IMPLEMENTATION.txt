Work from repository `sakaguchi5/SGE4` with baseline commit `afaed65d9400f3f80a7f5a9094edd9029082f95d`.

First read `AGENTS.md` and every file under `docs/spiral2-codex/`, including the complete extracted roadmap. Then verify the baseline and existing test environment.

Implement Spiral 2 through CU0-CU6. This is an implementation task, not a planning-only task. Proceed autonomously without a separate independent review agent. Keep the Independent Hierarchy Verifier as a mandatory software authority boundary.

Use the Stage order and evidence protocol in the pack. Diagnose and fix failures; do not weaken tests. Test existing Level 4 sufficiency before extending it. If a minimal Level 4 extension is proven necessary, document, version, canonically reconstruct, regress, and continue.

Do not ask for confirmation after each Stage. Do not push.

Complete Architecture, WARP, multi-frame, determinism, recovery, real-GPU measurement when available, and Decision Evidence. You may rank and recommend the next capability, but stop with:

NextCapabilitySelection = DeferredByOwner
SelectionStatus = OWNER_DECISION_REQUIRED

Do not create or begin the next capability.
